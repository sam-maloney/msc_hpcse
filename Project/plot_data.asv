N = 128;
file_name = 'density_serial.dat';

var_name = file_name(1:end-4);
data = load(file_name,'-ascii')

x = reshape(var_name(:,1),N,N)';
y = reshape(var_name(:,2),N,N)';
z = reshape(var_name(:,3),N,N)';
surf(x,y,z);